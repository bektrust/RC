#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Для Каждого ПараметрДубликат Из Параметры.Дубликаты Цикл
		Дубликат = Дубликаты.Добавить();
		ЗаполнитьЗначенияСвойств(Дубликат, ПараметрДубликат);
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Создать(Команда)
	
	Закрыть(Истина);
	
КонецПроцедуры

#КонецОбласти